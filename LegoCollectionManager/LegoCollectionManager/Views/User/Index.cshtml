@model LegoCollectionManager.Models.User

@{
    ViewData["Title"] = "Login";
}

<h1>Login</h1>

<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-controller="User" asp-action="Login" method="gett">
            <div class="form-group">
                <label asp-for=Username class="control-label" name="Username">Username</label>
                <input asp-for="Username" class="form-control" />
                <span asp-validation-for=Username class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for=Password class="control-label" name="Password">Password</label>
                <input asp-for="Password" class="form-control" type="password"/>
                <span asp-validation-for=Password class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Login" class="btn btn-primary" />
            </div>
        </form>
        @ViewData["IncorrectPassword"].ToString()
    </div>
</div>

<p>
    <a asp-action="Create">Register</a>
</p>