@model IEnumerable<LegoCollectionManager.Models.CustomSet>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.CustomSetName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PiecesAmount)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CustomSetImage)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CustomSetName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PiecesAmount)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CustomSetImage)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.CustomSetId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.CustomSetId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.CustomSetId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
